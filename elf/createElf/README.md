# 概要
入力してELFを作成する

# 詳細
```
$ python createElf.py
$ ls dump
dump
$ file dump 
dump: ELF 64-bit LSB executable, x86-64, version 1 (SYSV), statically linked, corrupted section header size
$ chmod a+x ./dump
$ ./dump
sh$
```
